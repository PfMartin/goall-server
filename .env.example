# PostgreSQL Configuration
POSTGRES_USER=goall
POSTGRES_PASSWORD=goall_secret
POSTGRES_DB=goall
POSTGRES_PORT=5432

# Database URL for golang-migrate and application
# This is automatically constructed by the Makefile from the variables above.
# Only set this if you need to override the connection string (e.g., for remote databases).
# DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable
